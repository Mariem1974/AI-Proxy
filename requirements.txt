# ============================================================
# AI-Proxy v2 — Python Dependencies
# ============================================================

# ── Web Framework ────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.20
python-dotenv>=1.0.1

# ── AI/ML — Classifiers ──────────────────────────────────────
# gliner 0.2.x requires transformers>=4.57.3 and tokenizers>=0.22
# chromadb 1.x dropped the tokenizers<=0.20 pin, so we use chromadb>=1.0
transformers>=4.57.3
huggingface-hub>=0.34.0

# CPU-only builds (~200 MB vs ~3 GB for default CUDA wheels)
# Requires --extra-index-url https://download.pytorch.org/whl/cpu in pip call
torch==2.5.1+cpu
torchvision==0.20.1+cpu
torchaudio==2.5.1+cpu

# ── AI/ML — PII Detection ────────────────────────────────────
gliner>=0.2.24

# ── NLP — Rule Engine ────────────────────────────────────────
spacy>=3.8.2
# Run: python -m spacy download en_core_web_sm

# ── Unicode / Entropy ────────────────────────────────────────
tiktoken>=0.7.0

# ── Document Processing ──────────────────────────────────────
python-doctr[torch]
PyMuPDF>=1.24.14
python-docx>=1.1.2
pypdf>=5.1.0
Pillow>=11.0.0

# ── Vector DB & RAG ──────────────────────────────────────────
# langchain 1.x + chromadb 1.x are co-released and share compatible tokenizers
langchain>=1.0.0
langchain-community>=0.4.0
langchain-text-splitters>=0.3.4
langchain-chroma>=1.0.0
chromadb>=1.0.0
sentence-transformers>=3.0.0

# ── LLM / Ollama ─────────────────────────────────────────────
ollama>=0.4.0
requests>=2.32.3
httpx>=0.27.0

# ── Database — PostgreSQL + TimescaleDB async ────────────────
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
sqlalchemy[asyncio]>=2.0.0
alembic>=1.13.0

# ── Auth — JWT + bcrypt ───────────────────────────────────────
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0

# ── Security & Alerting ──────────────────────────────────────
apprise>=1.9.2

# ── Data / Utilities ─────────────────────────────────────────
numpy>=1.26.4
pydantic>=2.10.3

# ── Testing ──────────────────────────────────────────────────
pytest>=8.3.4
pytest-asyncio>=0.24.0

# ============================================================
# System-level requirements:
#   - Ollama  : https://ollama.com/  (for qwen2.5:1.5b + llama3.2)
#   - Models  : ollama pull qwen2.5:1.5b && ollama pull llama3.2
#   - spaCy   : python -m spacy download en_core_web_sm
# ============================================================
